# Core dependencies. Pinning strategy:
#   - Exact pins for packages verified locally.
#   - Floor pins (>=) for torch / scikit-learn since they are provided on the
#     cluster (SDumont/ICA) and their exact versions differ from this machine.
#
# Install locally:
#   pip install -r requirements.txt
# Install cluster (GPU) torch separately after this file, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu<VER>

# --- ML framework ---
# CPU-only local dev is fine for smoke tests / math unit tests.
torch>=2.0.0

# --- Scientific / data ---
numpy==2.5.1
scikit-learn>=1.3

# --- Visualization ---
matplotlib==3.11.0

# --- Config parsing (config-driven runs) ---
pyyaml>=6.0

# --- Dev / test ---
pytest>=7.0
